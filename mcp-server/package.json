{
  "name": "distill-mcp",
  "version": "1.0.0",
  "type": "module",
  "description": "Distill MCP server — chat with your signals data from Claude Desktop",
  "bin": {
    "distill-mcp": "./build/index.js"
  },
  "scripts": {
    "build": "tsc && chmod 755 build/index.js",
    "dev": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.0",
    "@neondatabase/serverless": "^1.0.2",
    "drizzle-orm": "^0.45.1",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  },
  "files": ["build"]
}
